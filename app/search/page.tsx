import Link from "next/link";
export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const { q = "" } = await searchParams; return <main className="mx-auto min-h-screen max-w-4xl px-5 py-16"><Link href="/" className="text-sm underline">Back home</Link><h1 className="mt-8 text-4xl font-bold">Search</h1><p className="mt-3 text-slate-600">Query: <strong>{q || "—"}</strong></p><div className="mt-8 rounded-2xl border bg-white p-6"><p className="text-slate-600">Search infrastructure will combine exact identifier and model matching, PostgreSQL full-text search, and trigram fuzzy matching once the product database is connected.</p></div></main> }